x <- c(10.4, 5.6, 3.1, 6.4, 21.7)

# 示例1：选择非缺失值
y <- x[!is.na(x)]

# 示例2：选择x+1的值，其中x既非缺失又是正数
z <- (x+1)[(!is.na(x)) & x > 0]

# 示例3：选择第六个元素
x[6]

# 示例4：选择前10个元素
x[1:10]

# 示例5：重复选择元素
c("x","y")[rep(c(1,2,2,1), times=4)]

# 示例6：排除前五个元素
y <- x[-(1:5)]

# 示例7：通过名称选择元素
fruit <- c(5, 10, 1, 20)
names(fruit) <- c("orange", "banana", "apple", "peach")
lunch <- fruit[c("apple","orange")]

# 示例8：将缺失值替换为0
x[is.na(x)] <- 0

# 示例9：将负值取绝对值
y[y < 0] <- -y[y < 0]






